import{describe,expect,it}from"vitest";import{decideTaskRecovery}from"./queue-recovery.js";const now=Date.parse("2026-08-12T12:00:00Z");describe("queue recovery",()=>{it("ignores healthy work",()=>expect(decideTaskRecovery({status:"running",attempt:1,startedAt:"2026-08-12T11:55:00Z"},now).action).toBe("none"));it("retries stale leases with a new attempt",()=>expect(decideTaskRecovery({status:"running",attempt:1,maxAttempts:3,startedAt:"2026-08-12T11:30:00Z"},now)).toMatchObject({action:"retry",nextAttempt:2}));it("dead-letters exhausted work",()=>expect(decideTaskRecovery({status:"failed",attempt:3,maxAttempts:3},now).action).toBe("dead_letter"));it("recovers queued tasks never claimed",()=>expect(decideTaskRecovery({status:"queued",attempt:0,createdAt:"2026-08-12T11:00:00Z"},now).action).toBe("retry"))});
