import{describe,expect,it}from"vitest";import{eventTransitions,resolveWorkflowTransition,workflowDefinitions,workflowOrder}from"./workflow.js";
describe("company workflow",()=>{it("defines policy for every ordered stage",()=>{expect(Object.keys(workflowDefinitions)).toEqual(workflowOrder);for(const stage of workflowOrder){const policy=workflowDefinitions[stage];expect(policy.timeoutMinutes).toBeGreaterThan(0);expect(policy.retry.maxAttempts).toBeGreaterThan(0);expect(policy.notifications.length).toBeGreaterThan(0)}});it("advances only from allowed stages",()=>{expect(resolveWorkflowTransition("phone_call","LeadWon")).toEqual({from:"phone_call",to:"won"});expect(resolveWorkflowTransition("development","LeadWon")).toBeUndefined()});it("rolls QA failure back to development",()=>{expect(eventTransitions.QAFailed).toEqual({from:["qa"],to:"development"})});it("rolls missing assets back from every downstream production stage",()=>{expect(eventTransitions.AssetsMissing?.to).toBe("assets");expect(eventTransitions.AssetsMissing?.from).toContain("qa")});it("requires customer approval before deployment",()=>{expect(workflowDefinitions.customer_approval.approval).toBe("customer");expect(resolveWorkflowTransition("customer_approval","WebsiteApproved")?.to).toBe("deployment")})});
