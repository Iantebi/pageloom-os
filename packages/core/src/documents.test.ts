import{describe,expect,it}from"vitest";import{documentTemplateSchema,nextDocumentVersion,renderTemplate,signatureMatchesDocument,type DigitalSignature,type DocumentVersion}from"./documents.js";
describe("enterprise document engine",()=>{
  const template=documentTemplateSchema.parse({id:"proposal-he",type:"proposal",name:"הצעה",audience:"customer",version:1,status:"active",titleTemplate:"הצעה עבור {{customer.name}}",bodyTemplate:"שלום {{customer.name}}, היקף הפרויקט הוא {{scope}}.",requiredVariables:["customer.name","scope"]});
  it("renders strict Hebrew templates and rejects missing variables",()=>{expect(renderTemplate(template,{customer:{name:"נועה"},scope:"אתר תדמית"})).toMatchObject({title:"הצעה עבור נועה",direction:"rtl",locale:"he"});expect(()=>renderTemplate(template,{customer:{name:"נועה"}})).toThrow(/scope/)});
  it("increments immutable document versions",()=>{expect(nextDocumentVersion([])).toBe(1);expect(nextDocumentVersion([1,3,2])).toBe(4)});
  it("binds a digital signature to an exact active version hash",()=>{const document={id:"d1",organizationId:"o1",templateId:"t1",type:"proposal",audience:"customer",version:2,locale:"he",direction:"rtl",title:"הצעה",html:"<main>תוכן ההצעה ללקוח</main>",contentHash:"a".repeat(64),status:"signed",createdAt:"2026-08-16T00:00:00.000Z",createdBy:"u1"}satisfies DocumentVersion,signature={documentId:"d1",documentVersion:2,documentHash:document.contentHash,signerId:"c1",signerName:"נועה ישראלי",signerRole:"customer",intentText:"אני מאשרת את המסמך בגרסה זו",signedAt:"2026-08-16T00:01:00.000Z",ipHash:"b".repeat(64),userAgentHash:"c".repeat(64),signatureHash:"d".repeat(64)}satisfies DigitalSignature;expect(signatureMatchesDocument(signature,document)).toBe(true);expect(signatureMatchesDocument({...signature,documentVersion:1},document)).toBe(false)});
});
