import{describe,expect,it}from"vitest";import{calculateOperationsHealth}from"./operations-health.js";const now=Date.parse("2026-08-12T12:00:00.000Z");describe("operations health",()=>{it("reports healthy verified operations",()=>expect(calculateOperationsHealth({nowMs:now,tasks:[],workflows:[],approvals:[],usage:[]})).toMatchObject({status:"healthy",score:100}));it("detects customer-delivery blockers",()=>expect(calculateOperationsHealth({nowMs:now,tasks:[{status:"failed"}],workflows:[{status:"blocked"}],approvals:[],usage:[]})).toMatchObject({status:"critical",signals:{failedTasks:1,blockedWorkflows:1}}));it("detects stale queues, approvals, and missing rate cards",()=>{const report=calculateOperationsHealth({nowMs:now,tasks:[{status:"queued",createdAt:"2026-08-12T10:00:00.000Z"}],workflows:[],approvals:[{status:"pending",requestedAt:"2026-08-10T12:00:00.000Z"}],usage:[{pricingStatus:"unconfigured"}]});expect(report.status).toBe("degraded");expect(report.signals).toMatchObject({staleQueuedTasks:1,overdueApprovals:1,unpricedUsage:1})})});
