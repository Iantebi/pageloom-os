import type { AgentDefinition, ModelRoute, ModelProvider, Priority } from "./types.js";
export interface RoutingContext {agent:AgentDefinition;priority:Priority;requiresCode:boolean;requiresGoogleContext:boolean;budgetRemainingUsd:number;providerHealth:Record<ModelProvider,boolean>;}
export function routeModel(context:RoutingContext):ModelRoute {const provider=context.providerHealth.gemini?"gemini":context.providerHealth.openai?"openai":undefined;if(!provider)throw new Error("No healthy model provider is available");if(provider==="gemini")return{provider,model:"gemini-2.5-pro",reason:"organization primary model via Google AI Studio; OpenAI fallback enabled",maxOutputTokens:65536,estimatedMaxCostUsd:4};return{provider,model:"gpt-5.6-sol",reason:"OpenAI GPT fallback because the primary Gemini provider is unavailable",maxOutputTokens:65536,estimatedMaxCostUsd:8};}
