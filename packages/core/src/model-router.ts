import type { AgentDefinition, ModelRoute, ModelProvider, Priority } from "./types.js";
export interface RoutingContext {agent:AgentDefinition;priority:Priority;requiresCode:boolean;requiresGoogleContext:boolean;budgetRemainingUsd:number;providerHealth:Record<ModelProvider,boolean>;preferredProvider?:ModelProvider;}
export function routeModel(context:RoutingContext):ModelRoute {const preferred=context.preferredProvider??context.agent.preferredProvider,alternate:ModelProvider=preferred==="openai"?"gemini":"openai",provider=context.providerHealth[preferred]?preferred:context.providerHealth[alternate]?alternate:undefined;if(!provider)throw new Error("No enabled and healthy model provider is available");const estimatedMaxCostUsd=provider==="gemini"?4:8;if(context.budgetRemainingUsd<estimatedMaxCostUsd)throw new Error(`AI budget exhausted: ${provider} requires up to $${estimatedMaxCostUsd.toFixed(2)} and $${context.budgetRemainingUsd.toFixed(2)} remains`);return{provider,model:provider==="gemini"?"gemini-pro-latest":"gpt-5.6-sol",reason:provider===preferred?`${provider} selected by provider-independent API configuration`:`${provider} selected because configured provider ${preferred} is unavailable`,maxOutputTokens:65536,estimatedMaxCostUsd};}
