import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const source=readFileSync(new URL("./fleet-api.ts",import.meta.url),"utf8");describe("fleet API safety",()=>{it("turns every fleet mutation into an unexecuted approval",()=>{expect(source).toContain('status:"awaiting_approval",executed:false');expect(source).toContain("requireCeo");expect(source).not.toContain("projects.create(")})});
