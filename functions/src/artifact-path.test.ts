import{describe,expect,it}from"vitest";import{internalArtifactObject,projectArtifactObject,safeArtifactPath}from"./artifact-path.js";
describe("artifact paths",()=>{it("removes traversal and unsafe characters",()=>expect(safeArtifactPath("../../src/a file.tsx")).toBe("src/a-file.tsx"));it("places customer work inside its project boundary",()=>expect(projectArtifactObject("org","project","task","src/app.tsx")).toBe("organizations/org/projects/project/artifacts/task/src/app.tsx"));it("keeps non-project work staff-only",()=>expect(internalArtifactObject("org","task","report.md")).toBe("organizations/org/internal-artifacts/task/report.md"))});
