import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const source=readFileSync(new URL("./command-center-api.ts",import.meta.url),"utf8");describe("CEO command center API",()=>{it("requires an executive role",()=>expect(source).toContain('["owner","admin","operator"]'));it("covers financial, customer, delivery, fleet, risk and usage metrics",()=>{for(const field of["pendingInvoices","projectHealth","certificates","backupRuns","pendingApprovals","aiCostUsd","hostingBytes","storageBytes"])expect(source).toContain(field)})});
