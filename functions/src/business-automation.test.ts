import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const source=readFileSync(new URL("./business-automation.ts",import.meta.url),"utf8");describe("business automation delivery",()=>{it("creates idempotent internal owner notifications only",()=>{expect(source).toContain('audience:"owner"');expect(source).toContain("runTransaction");expect(source).not.toContain("sendMail");expect(source).not.toContain("whatsapp")});it("writes the alert's structured type and params alongside the English title/body, so a frontend dictionary can localize it",()=>{expect(source).toContain("type:alert.type");expect(source).toContain("params:alert.params");expect(source).toContain("title:alert.title");expect(source).toContain("body:alert.body")})});
