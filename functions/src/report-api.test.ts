import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const source=readFileSync(new URL("./report-api.ts",import.meta.url),"utf8");describe("business reports API",()=>{it("supports every report family and immutable PDF/CSV integrity",()=>{for(const value of["executive","monthly","customer","financial","infrastructure","support","growth","pdfHash","csvHash","REPORT_INTEGRITY_FAILURE"])expect(source).toContain(value)})});
