import{describe,expect,it}from"vitest";import{openAiFallbackEnabled,providerHealthForMode}from"./provider-mode.js";
describe("API provider mode",()=>{it("enables only the configured provider by default",()=>{expect(openAiFallbackEnabled(undefined)).toBe(false);expect(providerHealthForMode("openai",false)).toEqual({openai:true,gemini:false});expect(providerHealthForMode("gemini",false)).toEqual({gemini:true,openai:false})});it("requires an explicit true value to enable fallback",()=>expect(openAiFallbackEnabled(" TRUE ")).toBe(true))});
