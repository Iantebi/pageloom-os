import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const source=readFileSync(new URL("./website-content-api.ts",import.meta.url),"utf8");
describe("website content API security",()=>{it("authorizes every content route at the platform, organization, or project boundary",()=>{expect(source).toContain("requirePlatformProjectAccess");expect(source).toContain("requirePlatformOrRole");expect(source).not.toContain("allow write")});it("validates fields and blocks cross-website media paths",()=>{expect(source).toContain("validateContentFieldValue");expect(source).toContain("canEditContentField");expect(source).toContain("Media must belong to the same customer, project, and website")});it("implements draft, submit, decision, publish, revision, and rollback operations",()=>{for(const operation of["/draft","/submit","/decision","/publish","/rollback","contentRevisions"])expect(source).toContain(operation)})});
