"use client";import{useEffect,useState}from"react";import{ServerCog}from"lucide-react";import{api}from"@/lib/api";import{useOrganization}from"@/lib/organization";import{Card,CardHeader,Empty,Status,money}from"./product-ui";import{t}from"@/lib/i18n";type Resource={id:string;displayName:string;status:string;region:string;monthlyCost:number;hostingStatus:string;firestoreStatus:string;storageStatus:string;backupStatus:string;monitoringStatus:string;issues:string[]};type Fleet={total:number;healthy:number;atRisk:number;monthlyCost:number;backupFailures:number;resources:Resource[]};export function FleetOverview(){const{organizationId}=useOrganization(),[fleet,setFleet]=useState<Fleet>();useEffect(()=>{if(organizationId)api<Fleet>(`/fleet/${organizationId}`).then(setFleet).catch(console.error)},[organizationId]);if(!fleet)return null;const s=t("fleetOverview");return <Card><CardHeader icon={ServerCog} title={s.title} subtitle={s.subtitle(fleet.healthy,fleet.total,money(fleet.monthlyCost),fleet.backupFailures)}/>{fleet.resources.length?<div className="table-wrap"><table className="data-table"><thead><tr><th>{s.customerProject}</th><th>{s.region}</th><th>{s.hosting}</th><th>Firestore</th><th>{s.storage}</th><th>{s.backup}</th><th>{s.monitoring}</th><th>{s.cost}</th></tr></thead><tbody>{fleet.resources.map(item=><tr key={item.id}><td><b>{item.displayName}</b><Status value={item.status}/></td><td>{item.region}</td><td><Status value={item.hostingStatus}/></td><td><Status value={item.firestoreStatus}/></td><td><Status value={item.storageStatus}/></td><td><Status value={item.backupStatus}/></td><td><Status value={item.monitoringStatus}/></td><td>{money(item.monthlyCost)}</td></tr>)}</tbody></table></div>:<Empty title={s.emptyTitle} description={s.emptyDescription}/>}</Card>}
