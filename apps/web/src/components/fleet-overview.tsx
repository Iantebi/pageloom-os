"use client";import{useEffect,useState}from"react";import{ServerCog}from"lucide-react";import{api}from"@/lib/api";import{useOrganization}from"@/lib/organization";import{Card,CardHeader,Empty,Status,money}from"./product-ui";type Resource={id:string;displayName:string;status:string;region:string;monthlyCost:number;hostingStatus:string;firestoreStatus:string;storageStatus:string;backupStatus:string;monitoringStatus:string;issues:string[]};type Fleet={total:number;healthy:number;atRisk:number;monthlyCost:number;backupFailures:number;resources:Resource[]};export function FleetOverview(){const{organizationId}=useOrganization(),[fleet,setFleet]=useState<Fleet>();useEffect(()=>{if(organizationId)api<Fleet>(`/fleet/${organizationId}`).then(setFleet).catch(console.error)},[organizationId]);if(!fleet)return null;return <Card><CardHeader icon={ServerCog} title="Infrastructure fleet" subtitle={`${fleet.healthy}/${fleet.total} healthy · ${money(fleet.monthlyCost)} monthly · ${fleet.backupFailures} backup failures`}/>{fleet.resources.length?<div className="table-wrap"><table className="data-table"><thead><tr><th>Customer project</th><th>Region</th><th>Hosting</th><th>Firestore</th><th>Storage</th><th>Backup</th><th>Monitoring</th><th>Cost</th></tr></thead><tbody>{fleet.resources.map(item=><tr key={item.id}><td><b>{item.displayName}</b><Status value={item.status}/></td><td>{item.region}</td><td>{item.hostingStatus}</td><td>{item.firestoreStatus}</td><td>{item.storageStatus}</td><td>{item.backupStatus}</td><td>{item.monitoringStatus}</td><td>{money(item.monthlyCost)}</td></tr>)}</tbody></table></div>:<Empty title="No provisioned fleet resources" description="Approved customer infrastructure appears here after registration."/>}</Card>}
