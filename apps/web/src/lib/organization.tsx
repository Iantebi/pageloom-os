"use client";
import {createContext,useContext,useEffect,useMemo,useState} from "react";
import {api} from "./api";import {useAuth} from "./auth";
export type Membership={id:string;name?:string;role:"owner"|"admin"|"operator"|"member"|"client";customerId?:string;projectIds?:string[];websiteIds?:string[];platform?:boolean};
type Value={organizationId:string;organizations:Membership[];membership?:Membership;setOrganizationId:(id:string)=>void;loading:boolean;error:string;retry:()=>void};
const Context=createContext<Value|null>(null);
export function OrganizationProvider({children}:{children:React.ReactNode}){const{user}=useAuth();const[organizations,setOrganizations]=useState<Membership[]>([]);const[organizationId,setOrganizationId]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[revision,setRevision]=useState(0);useEffect(()=>{if(!user)return;let cancelled=false;const load=async()=>{for(let attempt=0;attempt<2;attempt++){try{const data=await api<{organizations:Membership[]}>("/me");if(!cancelled){setOrganizations(data.organizations);setOrganizationId(current=>data.organizations.some(item=>item.id===current)?current:data.organizations[0]?.id||"")}return}catch{if(attempt===1&&!cancelled)setError("We couldn’t load your workspace. Check your connection and try again.")}}};void load().finally(()=>{if(!cancelled)setLoading(false)});return()=>{cancelled=true}},[user,revision]);const value=useMemo(()=>({organizationId,organizations,membership:organizations.find(item=>item.id===organizationId),setOrganizationId,loading,error,retry:()=>{setLoading(true);setError("");setRevision(value=>value+1)}}),[organizationId,organizations,loading,error]);return <Context.Provider value={value}>{children}</Context.Provider>}
export function useOrganization(){const value=useContext(Context);if(!value)throw new Error("OrganizationProvider is missing");return value}
